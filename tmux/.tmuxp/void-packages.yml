session_name: void-packages
windows:
- window_name: dev
  layout: tiled
  shell_command_before:
    - cd $PROJECTS/thirdy-party/void-packages
    - clear
  panes:
    - git fetch --all && git reset --hard upstream/master && ./check-updates
